#!/usr/bin/env perl
use warnings;
use strict;
use FindBin qw($Bin);
use lib "$Bin/../lib";
use Unicoder::Search qw(unicoder_create_db
                        unicoder_save_db);
unicoder_create_db();
unicoder_save_db();
