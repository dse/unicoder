#!/usr/bin/env perl
use warnings;
use strict;
use JSON::XS;
use Unicode::UCD qw(prop_aliases);

our $JSON = JSON::XS->new()->pretty(1)->canonical(1)->ascii(1)->space_before(0);

foreach my $prop (@ARGV) {
    my $aliases = prop_aliases($prop);
    print($JSON->encode($aliases));
}
